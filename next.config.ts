import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['www.google.com', 'googleapis.com', 'lh3.googleusercontent.com', 'dev-shhjhxebflmbzgcw.us.auth0.com'],
  },
};

export default nextConfig;
